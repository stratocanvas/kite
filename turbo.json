{
	"$schema": "https://turbo.build/schema.json",
	"ui": "tui",
	"globalEnv": ["MONGODB_URI", "TIPTAP_TOKEN"],
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"inputs": ["$TURBO_DEFAULT$", ".env*"],
			"outputs": [".next/**", "!.next/cache/**"],
			"env": ["MONGODB_URI", "TIPTAP_TOKEN"]
		},
		"check-types": {
			"dependsOn": ["^check-types"]
		},
		"lint": {
			"dependsOn": ["^lint"]
		},
		"dev": {
			"cache": false,
			"persistent": true
		}
	}
}
